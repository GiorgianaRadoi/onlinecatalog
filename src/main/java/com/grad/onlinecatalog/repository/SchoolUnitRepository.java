package com.grad.onlinecatalog.repository;

import com.grad.onlinecatalog.model.SchoolUnit;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SchoolUnitRepository extends JpaRepository<SchoolUnit, Integer> {
}
